# 仓库内统一使用 LF。
#
# 这一条不是洁癖：docker-entrypoint.sh 会被拷进 Alpine 镜像直接执行，
# 如果它在工作区被 Git 转成 CRLF，容器的 shebang 会变成 "#!/bin/sh\r"，
# 启动时报 `/bin/sh^M: not found`，且极难从日志看出原因。
* text=auto eol=lf

*.sh            text eol=lf
Dockerfile      text eol=lf
*.go            text eol=lf
*.yml           text eol=lf
*.yaml          text eol=lf
*.md            text eol=lf
*.json          text eol=lf

# 二进制文件不做任何换行转换
*.png           binary
*.jpg           binary
*.jpeg          binary
*.gif           binary
*.ico           binary
*.zip           binary
